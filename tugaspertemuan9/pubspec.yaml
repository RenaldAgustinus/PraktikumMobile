name: project9
description: A new Flutter project.
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# ... (bagian version dan environment tidak diubah)
version: 1.0.0+1

environment:
  sdk: '>=3.1.3 <4.0.0'

# Dependencies specify other packages that your package needs in order to work.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.2
  
  # --- PACKAGE UNTUK TUGAS KAMERA ---
  camera: ^0.10.5+9 
  
  # 👇 DITAMBAHKAN: Untuk mendapatkan path direktori lokal (Poin 5 Tugas)
  path_provider: ^2.1.1 
  
  # 👇 DITAMBAHKAN: Untuk membantu mengelola path file secara cross-platform
  path: ^1.8.3
  
dev_dependencies:
  flutter_test:
    sdk: flutter
# ... (dev_dependencies dan bagian flutter: lainnya tidak diubah)